# ─────────────────────────────────────────────────────────────────────────────
# SENSIBLE — credenciales OAuth (client secret, access/refresh tokens).
# Nunca al repo. En GitHub Actions van como Secrets cifrados, reconstruidos
# en runtime dentro de credentials/.
# ─────────────────────────────────────────────────────────────────────────────
credentials/

# ─────────────────────────────────────────────────────────────────────────────
# SENSIBLE — métricas privadas del canal (vistas, subs, watch time, informes).
# ─────────────────────────────────────────────────────────────────────────────
data/analytics_history.json
reports/

# ─────────────────────────────────────────────────────────────────────────────
# Config local de Claude Code (incluye client_id OAuth en settings.local.json).
# ─────────────────────────────────────────────────────────────────────────────
.claude/

# ─────────────────────────────────────────────────────────────────────────────
# Logs de ejecución.
# ─────────────────────────────────────────────────────────────────────────────
logs/

# ─────────────────────────────────────────────────────────────────────────────
# Documentación interna — no publicar.
# ─────────────────────────────────────────────────────────────────────────────
MASTER_CONTEXT.md

# ─────────────────────────────────────────────────────────────────────────────
# Vídeos renderizados (regenerables, pesados; ya publicados en YouTube).
# ─────────────────────────────────────────────────────────────────────────────
output/

# ─────────────────────────────────────────────────────────────────────────────
# Python — bytecode y artefactos.
# ─────────────────────────────────────────────────────────────────────────────
__pycache__/
*.py[cod]
*$py.class
.Python
*.egg-info/
.venv/
venv/
env/

# ─────────────────────────────────────────────────────────────────────────────
# Sistema operativo.
# ─────────────────────────────────────────────────────────────────────────────
Thumbs.db
ehthumbs.db
Desktop.ini
.DS_Store

# ─────────────────────────────────────────────────────────────────────────────
# Champions — caché de históricos de Elo.
# ─────────────────────────────────────────────────────────────────────────────
# ~600 KB por club y unos 5 MB por jornada: es un espejo local de un endpoint
# público, se reconstruye solo y no aporta nada al historial del repo.
# data/clubelo_cache.json (la tabla del día) SÍ se versiona: es pequeño y actúa
# de red de seguridad cuando clubelo.com está caído.
data/clubelo_history_cache.json

# ─────────────────────────────────────────────────────────────────────────────
# LaLiga — caché de ESPN.
# ─────────────────────────────────────────────────────────────────────────────
# TTL de 6 h y sin lectura en caliente cuando caduca, asi que no sirve de red
# de seguridad como data/clubelo_cache.json: versionarlo solo seria ruido en
# cada render.
data/laliga_cache.json
